fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
librosa>=0.10.1
soundfile>=0.12.1
numpy>=1.26.0
pandas>=2.1.4
scipy>=1.11.4
noisereduce>=3.0.0
pydub>=0.25.1
python-multipart>=0.0.6
scikit-learn==1.8.0
joblib>=1.3.2
# xgboost is required at runtime: services/classification.py imports XGBClassifier
# and the deployed baby_cry_classifier .pkl is an XGBoost model.
xgboost>=3.2.0
# Exclude heavy training packages (torch, transformers, torchaudio, accelerate).
# They are only needed if the wav2vec2 model artifact is deployed — see requirements.txt.
